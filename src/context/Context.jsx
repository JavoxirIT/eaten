import { createContext } from "react";

export const AllBookingContext = createContext(null);
export const AllListingContext = createContext(null);
export const SitiesAndDistrictContext = createContext(null);
export const UnitContext = createContext(null);
export const PriorityContext = createContext(null);
export const ListingStatusContext = createContext(null);
